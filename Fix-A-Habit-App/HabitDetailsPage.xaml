<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HabitTrackerApp.OnboardingPage"
             BackgroundColor="#53a58b">

    <VerticalStackLayout 
        Padding="30"
        Spacing="25">

        <Label 
            Text="Welcome"
            FontSize="28"
            FontAttributes="Bold"
            TextColor="White"
            Margin="0,60,0,20"/>

        <!-- Feature List -->
        <Frame BackgroundColor="White" 
               CornerRadius="10" 
               Padding="15"
               HasShadow="False">
            <Label Text="📊 Track Your Progress" 
                   TextColor="#1a4d3d"
                   FontSize="16"/>
        </Frame>

        <Frame BackgroundColor="White" 
               CornerRadius="10" 
               Padding="15"
               HasShadow="False">
            <Label Text="🎯 Set Goals" 
                   TextColor="#1a4d3d"
                   FontSize="16"/>
        </Frame>

        <Frame BackgroundColor="White" 
               CornerRadius="10" 
               Padding="15"
               HasShadow="False">
            <Label Text="⏰ Daily Reminders" 
                   TextColor="#1a4d3d"
                   FontSize="16"/>
        </Frame>

        <!-- Next Button -->
        <Button 
            Text="NEXT"
            BackgroundColor="#2d8a6e"
            TextColor="White"
            FontAttributes="Bold"
            CornerRadius="25"
            HeightRequest="50"
            Margin="0,40,0,0"
            Clicked="OnNextClicked"/>

    </VerticalStackLayout>

</ContentPage>